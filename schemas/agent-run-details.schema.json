{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jarvis.local/schemas/agent-run-details.schema.json",
  "title": "AgentRunDetails",
  "description": "JSON blob stored in agent_runs.details for audit, prompt versioning, and retry context",
  "type": "object",
  "required": ["agent_slug", "intent"],
  "additionalProperties": true,
  "properties": {
    "agent_slug": {
      "type": "string",
      "description": "Agent id from config/agents/*.json"
    },
    "intent": {
      "type": "string",
      "description": "Resolved intent at dispatch time"
    },
    "prompt_ref": {
      "type": "string",
      "description": "Path to prompt file used for this run"
    },
    "prompt_version": {
      "type": "string",
      "description": "Semantic version from agent config at run time"
    },
    "routing_version": {
      "type": "string",
      "description": "config/routing.json version at run time"
    },
    "attempt": {
      "type": "integer",
      "minimum": 1,
      "description": "1-based attempt number (retries increment this)"
    },
    "error": {
      "type": "string",
      "description": "Last error message when status is failed or retrying"
    },
    "fallback_from": {
      "type": "string",
      "description": "Primary agent slug when this run used fallback_agent_id"
    }
  }
}
