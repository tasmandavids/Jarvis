{
  "name": "jarvis",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@jarvis/web",
    "build": "npm run build --workspace=@jarvis/web",
    "lint": "npm run lint --workspace=@jarvis/web",
    "validate:config": "node scripts/validate-config.mjs",
    "test:routing": "node scripts/test-routing.mjs",
    "vercel:link": "cd apps/web && npx vercel@latest link --yes --scope olune --project jarvis",
    "env:pull": "cd apps/web && npx vercel@latest env pull .env.local --yes",
    "env:push": "node scripts/sync-vercel-env.mjs",
    "env:ls": "cd apps/web && npx vercel@latest env ls"
  },
  "engines": {
    "node": ">=20"
  }
}
